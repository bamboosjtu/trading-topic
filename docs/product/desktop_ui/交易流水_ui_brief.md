# 交易流水 UI Brief

> 状态：已审定设计 ｜ 页面入口：`实盘 / 交易流水` ｜ 效果图：同目录 `交易流水.png`
>
> 视觉规范（字号、颜色、圆角、间距、表格密度、16:9 与大屏自适应）统一遵循 [DESIGN_SYSTEM.md](../DESIGN_SYSTEM.md)，本文不再重复。

## 页面目标

R1 实盘唯一事实写入页：手工录入买入、卖出、分红、资金转入/转出、国债逆回购；查询核对历史；以追加式修正/冲正纠错；为持仓明细、账户资产与收益日历提供统一事实源。

数据流：交易流水（唯一事实源）→ 重建持仓/成本/现金/累计分红 → 持仓明细与收益日历只读展示。

R1 边界：单账户无切换；不支持导入券商流水；业务时间只精确到日期（YYYY-MM-DD）；不要求流水号、订单号、对手方或精确录入时间；内部 ID 不上界面；原记录不物理删除；持仓与收益不在本页由 Renderer 重算。

## 页面结构

```text
标题 + 用途说明                         数据截止日期 / 录入交易（唯一主按钮）
筛选区：日期范围 · 交易类型 · 标的类型 · 标的 · 备注 · 重置 · 查询
统计指标带：记录总数/累计买入/累计卖出/累计分红/资金净流入
交易记录表格 ＋ 交易详情 Drawer（右开 380–460px，表格自适应缩窄）
录入交易 Modal
底部：手动录入事实源提示
```

## 字段与指标口径

- 指标带默认统计当前筛选结果：记录总数；累计买入（含费用，表实际资金流出）；累计卖出（含费用，取领域层定义的卖出净流入）；累计分红（分红到账合计）；资金净流入 = 外部转入 − 外部转出（不把证券买卖、分红重复计入）。所有汇总由领域层或查询服务返回。
- 表格列：日期、标的（无标的 `—`）、交易类型（低饱和 Tag，仅分类不表涨跌）、数量/份额、价格、金额、手续费、备注（超长省略 + Tooltip）、查看。
- 默认排序：业务日期降序，同日按内部创建顺序稳定排序，排序键不外显。

## 关键交互

- 录入 Modal 按类型动态显示字段：
  - 买入/卖出：标的、数量、价格、手续费；
  - 现金分红：标的、到账金额，可选每股分红/登记日/到账日；
  - 资金转入/转出：金额；
  - 国债逆回购：代码/品种、本金、到期日、到期金额。
- 成交金额只读自动计算；提交前显示“本次影响摘要”：可用现金、标的数量、持仓成本、累计分红、待到账资产等变化。摘要由领域层预校验返回，Renderer 不重复实现账本公式。
- 校验由领域层负责：金额/价格/数量有限且非负、精度合法；卖出数量不得超过可用持仓；到账日不早于登记日；逆回购到期不早于成交日。失败时 Modal 保持打开、保留输入并聚焦错误字段。
- 修正/冲正只能从原记录详情发起：
  - “追加修正”预填原记录，提交后新增修正记录，不覆盖原记录；
  - “冲正”二次确认后新增反向影响记录，不物理删除；
  - 已冲正原记录与关联修正/冲正记录需明确状态，禁止重复冲正。
- 详情 Drawer 展示通用字段 + 类型扩展字段，不展示流水号、内部 ID、固定来源和精确录入时间。
- 分页默认 20 条/页，可选 20/50/100；表头固定；行内不提供直接编辑或删除。

## 状态与异常

- Loading：首载指标带 Skeleton、表格 Loading；查询切换保留筛选、局部 Loading；保存按钮显示进度并阻止重复提交。
- 空：无任何流水显示“暂无交易流水” + “录入第一笔交易”；筛选为空显示“当前条件下没有匹配记录”。
- 保存失败：保留 Modal 与用户输入，显示可操作错误；不插入半条记录。
- 重建失败（流水已存但持仓重建失败）：属严重一致性错误，页面持续 Alert，禁止展示伪完整持仓或收益，引导修正事实记录；原始流水不得静默丢失。

## 验收标准

- 无账户切换、券商导入、流水号和精确到时分秒的业务时间；“录入交易”是唯一事实写入口。
- 修正/冲正只从原记录发起，采用追加记录且不覆盖/删除原记录；重复冲正被阻止。
- 指标带 5 项、表格列、详情字段与“本次影响摘要”均由领域口径支撑，不在 Renderer 重建资产。
- 1920×1080 下筛选、指标带与 ≥10 行记录首屏完整可见；4K 保持字号尺度、扩展列宽与行数。
- Loading/空/筛选无结果/保存失败/部分数据/重建失败均有明确状态。
