# UI 设计评审概览

**任务**：评审 `src/desktop` 现有 UI 设计并提出优化方案，只分析、不改代码。  
**交付**：`docs/product/ui_design_review.md`（完整报告）  
**日期**：2026-07-26

## 主要发现

1. **单页质量高，但存在两套设计系统冲突**  
   历史回测页已落地统一、专业的冷蓝金融工作站视觉；同时代码中并存一套暖金/瓷色的 Tailwind 主题及三个未接入路由的页面（`AccountPage`、`LedgerPage`、`SettingsPage`），且引用了未定义的 CSS 类。这是当前最大的设计债务。

2. **可访问性有短板**  
   大量 10–11px 辅助文字及 Modal 9px 表格文字对比度约 3.2–3.9:1，未达 WCAG AA 普通文本 4.5:1；主色 `#1677ff` 在白色上仅 4.10:1，略低于标准。

3. **响应式断点不匹配**  
   `body { min-width: 1100px }` 与历史回测参数区实际所需宽度（约 1534px）不一致，导致 1100–1536px 窗口出现内部横向滚动；Modal 固定尺寸在 720px 最小高度下可能溢出。

4. **若干 UX 细节**  
   费用模式 Select 仅一项、图表“最大”按钮语义不清、顶栏设置入口重复。

## 优化优先级

| 优先级 | 方向 | 关键动作 |
|---|---|---|
| P0 | 统一设计系统 | 冻结/移除暖金主题，清理未接入页面或补齐样式，文档同步 |
| P1 | 可访问性 | 提升辅助文字对比度，最小字号 11–12px，修复主色文本对比度 |
| P1 | 响应式 | 重设 min-width、参数区折行、Modal 适配 |
| P2 | 组件化与设计 token | 拆分 `BacktestPage`，提取 CSS 变量，对齐 Tailwind 工具类 |
| P2–P3 | UX 细节 | 费用模式改为只读、重命名“最大”、统一顶栏入口、动态图表标题 |

## 下一步建议

先让用户确认：暖金系统是否保留、窗口最小宽度是否接受 1280px、Modal 表格是否改为 12 条/页以换取更大字号。然后按 P0→P1→P2 顺序实施。
