# AGENTS.md

## 适用范围

本文件适用于整个仓库。若子目录以后增加自己的 `AGENTS.md`，以距离目标文件最近的规则为准。

本项目是投资研究实验室，兼顾学习、研究与开发。默认使用中文沟通和写作；代码标识符、协议名及行业通用缩写可保留英文。

Labs、Research、Src 是三个独立生命周期的域。它们共享经过评审的结论，不共享业务源码、数据目录或运行环境。三个域各自存在相似的回测算法或数据适配器是隔离带来的有意重复，不构成“应抽取共享核心”的代码异味。Research 不是产品运行库，产品也不是 Research 的发布包装。

- Labs 负责探索；
- Research 负责可复现地形成证据；
- Src 依据评审结论独立实现产品行为；

## 开始工作前

1. 先阅读根目录 `README.md`，再阅读任务所在目录的 README、研究说明或产品文档。
2. 检查 `git status --short`，保留用户已有改动，不清理、不覆盖无关文件。
3. 明确任务属于学习实验、可复现研究、最终成稿还是产品开发，并遵守对应目录边界。
4. 涉及投资结论时，先确认证券、市场、币种、报告期、数据截止日和复权/收益口径。

## 目录职责

| 路径                | 职责                               | 约束                                                                                                        |
| ------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `labs/`             | 研究主线、Notebook、探索性代码     | 每个研究主题收敛为一个 Lab，目录形如 `labs/00_金融数据获取/`、`labs/01_银行股定投回测/`，含子文档与 `data/` |
| `labs/data/`        | Lab 0 共享的原始数据和处理中间数据 | 大文件、可再生文件和含授权限制的数据默认不提交；Lab 自带数据写入各自子目录的 `data/`                        |
| `research/<topic>/` | 一个主题的可复现研究闭环           | 独立 `pyproject.toml`、`uv.lock`、`src/`、`tests/`、`data/` 与 `report/`，不依赖 Labs 环境                  |
| `src/desktop/`      | 攒股收息桌面产品                   | 纯 Node.js/TypeScript；不 import、执行或读取 Labs、Research                                                 |
| `reports/`          | 面向读者的最终研究成稿             | 不放通用代码、缓存和临时数据                                                                                |
| `docs/product/`     | 产品需求、架构和决策记录           | 文档必须包含范围、非目标和验收标准                                                                          |
| `.agents/skills/`   | 投资研究 Skill 唯一源目录          | 受版本控制；使用相关 skill 前完整阅读其 `SKILL.md`                                                          |
| `.claude/skills/`   | Claude Code 本地兼容入口           | 由 `.agents/sync-claude-skills.ps1` 生成 Junction，不单独维护                                               |

不要重新创建含义模糊的根目录 `report/`。可复现研究放入 `research/<topic>/`，最终文章放入 `reports/`。

## 数据与安全

- `.env`、Token、Cookie、账号和个人信息不得提交、打印到日志或写入报告。
- 只有任务确实需要时才读取环境变量；错误信息中也要避免泄露密钥。

## 开发与验证

各域独立管理环境。

修改代码时：

1. 优先修复根因，避免在报告中手工覆盖错误输出。
2. 将纯计算与数据获取、绘图和写文件分离，便于测试。
3. 为金融口径和边界条件增加小型确定性测试。
4. 目录移动后同步修正文档、命令、导入和产物路径。
5. Labs、Research、Src 只共享文档化结论和复制后的验收向量，不共享业务源码、数据目录或运行环境。
6. 三个域为保持隔离而各自实现同类算法或数据适配器属于预期，不得以“消除重复”为由建立跨域共享业务核心。
7. 目标域测试必须驱动本域实现，不能只断言另一个域的报告数字存在，也不能在测试时导入或运行另一个域。

桌面产品提交前至少执行：

```powershell
Set-Location src/desktop
npm run typecheck
npm test
npm run build
```

## 使用仓库 skills

任务明确匹配 `.agents/skills/<name>/SKILL.md` 时，应使用最小且相关的 skill 组合，并遵守其中的数据验证与输出要求。Skill 的报告路径若指向 `reports/`，继续沿用该约定。

Skills 是工作流，不是证据来源。即使 skill 给出了分析框架，事实与数字仍须按本文件的研究质量规则独立核验。

## 完成标准

交付前至少确认：

- 文件位于正确目录，README 或索引可找到它；
- 没有遗留旧路径、机器绝对路径、密钥或无意提交的大型生成文件；
- 相关测试、校验或最小运行命令已经执行；
- 代码输出、图表和报告关键数字一致；
- `git diff` 只包含任务范围内的变更；
- 最终说明列出变更、验证结果及仍未消除的风险。
