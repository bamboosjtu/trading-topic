# 代码质量与架构复核记录

> 复核日期：2026-07-26
>
> 范围：`src/desktop/`、`docs/product/`、根目录 `AGENTS.md`
>
> 结论：原评审包含已经失效的事实和一项与三域隔离原则冲突的建议，本轮已校正并处理当前可确认的问题。

## 1. 架构结论

Labs、Research、Src 是三个独立生命周期的域。它们共享经过评审的结论和复制后的验收向量，不共享业务源码、数据目录或运行环境。

因此，三个域各自存在相似的回测算法或数据适配器是隔离带来的有意重复，不构成“应抽取共享核心”的代码异味。Research 不是产品运行库，产品也不是 Research 的发布包装。正确关系是：

- Labs 负责探索；
- Research 负责可复现地形成证据；
- Src 依据评审结论独立实现产品行为；
- 跨域验证使用带来源提交、截止日、口径版本和预期结果的最小复制向量；
- 目标域测试必须驱动本域实现，禁止在测试时导入、执行或读取另一个域。

## 2. 原评审勘误

| 原结论 | 复核结果 | 处理 |
| --- | --- | --- |
| `src/desktop/sidecar/` 是未集成死代码 | 已失效，当前仓库不存在该目录 | 文档继续明确 R1 无 Python sidecar |
| Labs↔Research 源码重复违反隔离 | 错误；独立实现是隔离原则的预期结果 | 已更新 `AGENTS.md` 与架构文档 |
| 应由 Research 作为单一业务源码并做跨域运行时回归 | 与隔离原则冲突 | 改为复制验收向量，各域独立测试 |
| 桌面数据层缺少 mock | 已失效；`tencent.test.ts` 使用 `fetch` mock | 保留现有测试 |
| 数据库缺少导出恢复 round-trip | 已失效；`database.test.ts` 已覆盖恢复后流水 | 保留并继续作为持久化基线 |
| `react-router-dom` 未使用 | 错误；当前路由和导航正在使用 | 保留依赖 |

## 3. 本轮已修复

### 3.1 代码与依赖

- 删除没有产品调用方的 `simulateBacktestDetail`、对应契约及整组重复测试；明细继续由 `simulateBacktestSimple` 转换主回测流水，避免维护第二套算法。
- 删除只读取 Research 汇总 JSON、却不驱动产品计算的伪测试和快照；保留产品域确定性金融测试。
- 新增产品内部 `shared/constants.ts`，集中管理口径版本和支持的银行清单；main、数据适配器、存储和 renderer 不再各自硬编码。
- 腾讯行情缓存参数改为动态时间戳，移除固定随机值。
- XIRR 年基统一为 `365.25`。
- 删除未使用的 `zustand`；保留实际使用的 `react-router-dom`。
- 把 sidecar/drawer 等过期注释改为当前 Node.js/模态框实现。

### 3.2 界面与路由

- 导航收敛为“研究 / 历史回测”、“实盘 / 持仓明细、交易流水、分红日历”、“系统 / 设置”。
- 删除无入口的占位路由和页面元数据；未完成页面继续使用统一骨架屏。
- 历史回测参数区收敛为一行，高级日期与买入日进入弹层。
- K 线增加标的切换，默认日 K，均线为 MA5/MA10/MA20/MA60。
- 对比表按 XIRR 排序并移除重复列、重复导出入口。
- 明细模态框按 1920×1080 基线向上对齐，维持每页 20 条、事件筛选和固定时间顺序。

### 3.3 文档

- `AGENTS.md` 明确有意重复、跨域验收向量规则、产品验证命令和当前导航。
- `ARCHITECTURE.md`、`PRODUCT_BRIEF.md` 和历史回测 UI brief 与当前 R1 范围、路由和交互保持一致。

## 4. 仍保留的工程取舍

| 项目 | 当前判断 |
| --- | --- |
| `sql.js` 每次写操作导出完整数据库 | R1 本地低数据量可接受；数据量与写入延迟达到证据阈值后再迁移原生 SQLite |
| IPC 错误当前以普通 Error 传递 | R1 可用；后续表单增多时再统一错误码和字段级错误协议 |
| `BacktestPage.tsx` 仍偏大 | 已先删除重复算法、集中常量并收敛页面结构；后续可按图表、对比表、明细模态框拆分，不应与本轮视觉修复混成跨域重构 |
| Labs/Research 中存在固定历史区间 | 只要文档明确数据截止日，它属于可复现研究窗口，不是产品运行时配置缺陷 |
| Electron 最小宽度 1920px | 当前为明确设计基线而非兼容性疏漏；若未来支持小屏，需要单独设计响应式规格 |

## 5. 验收清单

- `npm run typecheck`
- `npm test`
- `npm run build`
- 1920×1080 检查历史回测主页面、三类图表、标的切换、明细打开/关闭、事件筛选和分页
- 检查 `src/desktop/` 不引用、执行或读取 Labs、Research
- 检查 Git diff 不包含生成物、机器绝对路径或无关改动
