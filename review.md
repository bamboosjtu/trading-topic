# 攒股收息 R1 发布前复核

> 评审日期：2026-07-30
>
> 范围：`src/desktop/` 架构设计、代码质量、金融口径与 `docs/product/` 一致性
>
> 原则：Labs / Research / Src 保持隔离；MVP 不迁移、不兼容旧代码、旧数据库或旧备份

## 结论

本轮提出的 P0-1、P0-2、P1-1、P1-2 均属实，现已修复并加入反向测试。结合上一轮行情尾部、ETF provenance、年度日历生命周期和跨日收益归因修复，当前未发现仍阻断 R1 的已知 P0/P1。

核心结论：

- 日度区间收益、累计净投入和 XIRR 已共用同一套证券投资现金流投影；
- 当前年度日历缺失不再锁死本地数据库，历史浏览、备份、日志和设置保持可用；
- 关联流水修正不能破坏分红再投入组；
- 备份在覆盖事务前执行完整领域校验，非法输入不会触碰现有数据库；
- 当前 Schema 2 的发布 fingerprint 是唯一基线，没有迁移、旧字段补齐、资产类型猜测或旧备份兼容分支；
- 产品代码未 import、执行或读取 Labs / Research，三条生命周期边界保持完整。

受控真实联网 smoke 未在本轮执行，不能把离线测试通过解释为外部数据源当前可用。

## 问题复核与修复

### P0-1：待再投入分红未统一进入净投入和 XIRR

**复核：属实，已修复。**

新增 `InvestmentCashProjection`，从当前有效投资事实统一投影：

- `externalBuySpend`：独立买入全额，加上关联买入超过可用分红的差额；
- `externalDividendIncome`：仅独立分红；
- `pendingReinvestmentCash`：关联分红减去关联买入已消耗金额；
- `externalCashflows`：供 XIRR 使用的投资者与组合之间现金流；
- `internalFundingByBuy`：供日度收益归因识别内部再投入。

统一规则：

1. 关联分红到账时进入组合内部待再投入现金，不减少外部净投入，也不形成 XIRR 正现金流；
2. 关联买入优先消耗该组分红，超额部分才是外部投入；
3. 未使用余额继续作为投资资产；
4. XIRR 期末资产为持仓市值加待再投入分红现金；
5. 修正或冲正后从当前有效事实重算，不持久化派生余额。

持仓页和 XLSX 已增加“待再投入分红”，累计净投入帮助文案改为外部资金口径。流水筛选汇总即使只显示关联组一侧，也使用完整有效组判断内部资金。

测试覆盖：

- 分红到账尚未再投入；
- 跨日全额再投入；
- 部分再投入并保留余额；
- 再投入超过分红只计算补足差额；
- 关联分红或买入被冲正；
- 待再投入现金加入期末 XIRR 资产；
- 价格变化时区间收益、净投入和 XIRR 采用同一内外部资金定义。

### P0-2：年度日历缺失导致应用无法启动

**复核：属实，已修复。**

原启动路径在数据库打开和窗口创建前执行年度 official 断言，确实会让跨年未升级用户无法访问本地数据。

门禁现已拆分：

- 构建/发布：`prebuild` 执行 `check:market-calendar`，当前年度不是 official 时失败；
- 运行启动：不再执行日历断言，先正常打开数据库和创建窗口；
- 运行提示：主界面对当前年度 pending 或缺失显示高可见错误告警，设置页保留年度状态与来源；
- 联网能力：行情 Provider 在请求涉及 pending/缺失年度之前明确拒绝。

因此流水浏览、已保存历史实验、已结束官方年度的收益日历、JSON 备份、日志导出、设置和诊断不再被日历门禁连带阻断。缺失年度的行情补齐、日期确认和新回测仍会安全失败。

### P1-1：修正可破坏关联组

**复核：属实，已修复。**

关联修正现在先继承原 `linkedGroupId`，再把“原事实冲正 + 修正后事实 + 其余有效事实”交给完整账本归约。组级校验要求：

- 只允许分红和买入；
- 同一标的；
- 同一资产类型；
- 分红日期不晚于再投入日期；
- 最多一个有效分红和一个有效买入。

传入其他关联编号会直接拒绝；独立事实不能通过修正加入关联组；普通单条录入也不能伪造关联编号。R1 当前不提供“解除关联”操作，因此用户不能通过通用修正静默制造失真关系。

冲正组内单条事实仍是合法审计语义：只剩分红时成为待再投入现金，只剩买入时成为外部投入。

### P1-2：备份校验过浅

**复核：属实，已修复。**

新增 `validateBackup()`，并保证在任何 `DELETE` / `INSERT` 覆盖事务前完成：

- 流水：唯一 ID、严格日期和时间、价格/金额/正整数数量、费用、修正和冲正引用、重复冲正、关联组、历史卖空及 `reduceLedger()` 完整性；
- 回测：实验/结果唯一 ID、请求、口径版本、状态、日期区间、指标有限数、交易、价格序列、权益曲线、OHLCV、来源及实验归属；
- 行情：证券代码、正有限价格、来源、复权、获取时间、请求区间、数据截止、覆盖与价格行一致性、重复主键；
- 公司行动：JSON 可解析、日期身份一致及数值结构；
- 证券目录：唯一代码、资产类型、来源、兜底原因和获取时间；
- 工作区：请求、图表字段、标的和活动实验引用。

反向测试逐类篡改当前 Schema 备份，并确认拒绝后目标库原有流水仍在、实验表仍未被覆盖。安全备份继续保留，但不再作为接受非法输入的替代品。

## 架构与代码质量

### 符合 R1 设计

- Electron 继续启用 `contextIsolation`、禁用 `nodeIntegration`、启用 sandbox；
- Renderer 只通过受限 IPC 使用本地能力，不接触 SQLite 和文件系统；
- 金融计算、数据适配、服务编排、存储与导出边界清晰；
- `InvestmentCashProjection` 消除了同一产品域内三套资金定义漂移，但没有建立跨 Labs / Research / Src 的共享业务核心；
- 分红待再投入余额是可重建分析状态，不新增证券账户现金或持久化冗余状态；
- 回测的 `incomplete` 行情仍在计算和保存前终止，不会保存伪 `completed` 实验；
- Schema 2 同时校验本轮发布 fingerprint 与实际 DDL shape；备份也携带并校验同一 fingerprint，不兼容旧库、同版本旧结构或修复前 Schema 2 备份。

### 发布后可关注

- `appService.ts`、`database.ts` 和备份验证器职责较多；R1 后可按用例拆分，但当前不值得为形式重构扩大发布风险；
- R1 尚无证券级停牌证据，因此严格回测继续保守拒绝未确认尾部，不能沿用前收盘伪造交易日价格；
- 年度官方休市安排仍需每年维护。构建门禁能阻止发布遗漏，但已安装旧版本只会降级和告警，不会自动获得新日历；
- “解除分红再投入关联”不是 R1 功能；现阶段所有可能破坏关系的通用修正均被拒绝。

## 文档一致性

已同步：

- `docs/product/PRD_R1.md`
- `docs/product/ARCHITECTURE.md`
- `docs/product/desktop_ui/持仓明细_ui_brief.md`
- `docs/product/desktop_ui/交易流水_ui_brief.md`
- `docs/product/desktop_ui/本地设置_ui_brief.md`
- `src/desktop/README.md`

文档现已明确统一外部现金流口径、待再投入分红展示与 XIRR 期末资产、关联修正约束、恢复前深校验，以及“构建失败但运行时正常启动并局部阻断”的年度日历生命周期。

## 验证

在 `src/desktop/` 执行：

```powershell
npm run typecheck
npm test
npm run build
git diff --check
```

结果：

- TypeScript 类型检查通过；
- 17 个离线测试文件、156 个测试通过；
- 1 个受控真实联网 smoke 测试按默认配置跳过；
- 构建通过，且 2026 年官方交易日历 `prebuild` 门禁通过；
- `git diff --check` 通过。

发布前仍须在允许联网的受控环境执行：

```powershell
npm run smoke:market-data
```

并人工审阅 `artifacts/market-data-smoke.json`。默认跳过不构成联网验证证据。
